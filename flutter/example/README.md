# Wiretalk Flutter Demo

```bash
cd sdk/flutter/example
flutter pub get
# Edit lib/main.dart — set baseUrl, widgetKey, appId
flutter run
```

Add `app:com.wiretalk.demo` to Widget Settings → Allowed Domains.

The example depends on the local `wiretalk_chat` package (`path: ../`).
